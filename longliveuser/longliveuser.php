<?

function getUserCh($userid){
  // Should return a curl handle(from curl_init) that represents
  // the LOGGEDIN user.
  // else, return a falsy value
}

function getUserChItr(){
  // Similar to getUserCh
  // return User 0's at the first call, User 1's at the second call etc.
}